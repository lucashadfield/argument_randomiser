from setuptools import setup, find_packages

setup(
    name='argument_randomiser',
    version='0.0.1',
    author='Lucas Hadfield',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
)
